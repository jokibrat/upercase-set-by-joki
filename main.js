function convertToUpperCase(str) {
  return str.toUpperCase();
}

// Example usage:
let originalString = "Hello, World!";
let convertedString = convertToUpperCase(originalString);
console.log(convertedString); // Output: HELLO, WORLD!